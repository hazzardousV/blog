<div id="header"><h1>Launch</h1></div>
<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag), class: 'tag_title'%>
    </li>
  <% end %>
</ul>

<%= link_to "<< Back to Articles List", articles_path %>
<div id="footer">
December 23rd,
	Quin Weidner
</div>